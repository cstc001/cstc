machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/.ethash" # Cache the ethash DAG generated by hive for consecutive builds
    - "~/.docker" # Cache all docker images manually to avoid lengthy rebuilds
  override:
    # Restore all previously cached docker images
    - mkdir -p ~/.docker
    - for img in `ls ~/.docker`; do docker load -i ~/.docker/$img; done

    # Pull in and hive, restore cached ethash DAGs and do a dry run
    - go get -u github.com/karalabe/hive
    - (cd ~/.go_workspace/src/github.com/karalabe/hive && mkdir -p workspace/ethash/ ~/.ethash)
    - (cd ~/.go_workspace/src/github.com/karalabe/hive && cp -r ~/.ethash/. workspace/ethash/)
    - (cd ~/.go_workspace/src/github.com/karalabe/hive && hive --docker-noshell --client=NONE --test=. --sim=. --loglevel=6)
    # Generating DAG
    - (cd ~/cstceumj && ./gradlew run -PmainClass=org.cstceum.Start -PjvmArgs="-Dethash.dir=~/.go_workspace/src/github.com/karalabe/hive/workspace/ethash -Dethash.blockNumber=0 -Xmx2G")
    # timeout 20 min
    - (cd ~/.go_workspace/src/github.com/karalabe/hive && hive --docker-noshell --client=cstceumj:local --test=NONE --sim=NONE --loglevel=6):
        timeout: 1200

    # Cache all the docker images and the ethash DAGs
    - for img in `docker images | grep -v "^<none>" | tail -n +2 | awk '{print $1}'`; do docker save $img > ~/.docker/`echo $img | tr '/' ':'`.tar; done
    - cp -r ~/.go_workspace/src/github.com/karalabe/hive/workspace/ethash/. ~/.ethash
    # Cache gradle artifacts
    - (cd ~/cstceumj && ./gradlew compileJava)
    - (cd ~ && git clone --depth 1 -b develop https://github.com/cstc-camp/cstceum-harmony.git)
    - (cd ~/cstceum-harmony && ./gradlew dependencyManagement)

test:
  override:
    # Build app and move into a known folder
    - (cd ~/.go_workspace/src/github.com/karalabe/hive/clients/cstceumj\:local && ./build.sh ~/cstceumj ~/cstceum-harmony ~)

    # Run hive and move all generated logs into the public artifacts folder
    - (cd ~/.go_workspace/src/github.com/karalabe/hive && hive --docker-noshell --client=cstceumj:local --override=$HOME/harmony.cstc.camp.tar --smoke --loglevel=3):
        timeout: 4000
    - (cd ~/.go_workspace/src/github.com/karalabe/hive && hive --docker-noshell --client=cstceumj:local --override=$HOME/harmony.cstc.camp.tar  --test=. --loglevel=3):
        timeout: 4000
    - cp -r ~/.go_workspace/src/github.com/karalabe/hive/workspace/logs/* $CIRCLE_ARTIFACTS

